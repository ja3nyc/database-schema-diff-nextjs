{
  "name": "db-schema-diff-preview",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.9"
  },
  "dependencies": {
    "pg-mem": "^2.8.1",
    "pgsql-ast-parser": "^12.0.1"
  },
  "packageManager": "npm@10.8.1"
}
